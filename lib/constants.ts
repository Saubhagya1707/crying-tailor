/**
 * App-wide constants
 */

export const APP_NAME = "ResumeTailor";

/** Main content max-width class for narrow pages (e.g. forms, history). */
export const CONTENT_WIDTH_NARROW = "max-w-2xl";
/** Main content max-width class for wide pages (e.g. dashboard). */
export const CONTENT_WIDTH_WIDE = "max-w-4xl";
